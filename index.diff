diff --git a/index.html b/index.html
index cf123d424d9e2da4c4228d6db480d6e6ea8178a5..1c4557556a1e960e6f5808f7f205d41f710b691c 100644
--- a/index.html
+++ b/index.html
@@ -225,72 +225,105 @@
       }
       45% {
         box-shadow:
           0 0 22px rgba(255,235,150,0.85),  /* 핵심 */
           0 0 44px rgba(255,220,130,0.55),
           0 0 80px rgba(255,220,130,0.35),
           0 0 120px rgba(255,220,130,0.20),
           0 10px 24px rgba(0,0,0,0.75);
       }
       100% {
         box-shadow:
           0 0 14px rgba(255,215,120,0.45),
           0 0 30px rgba(255,215,120,0.32),
           0 0 60px rgba(255,215,120,0.22),
           0 10px 24px rgba(0,0,0,0.75);
       }
     }
 
 
     .option-box.auto-hit-flash {
       animation: autoHitFlash 0.9s ease-out;
     }
 
 
 
-    @keyframes fadeInFast {
-      from { opacity:0; transform:translateY(4px); }
-      to   { opacity:1; transform:translateY(0); }
-    }
-    .fade-in-fast {
-      animation:fadeInFast 0.18s ease-out;
-    }
+    @keyframes fadeInFast {
+      from { opacity:0; transform:translateY(4px); }
+      to   { opacity:1; transform:translateY(0); }
+    }
+    .fade-in-fast {
+      animation:fadeInFast 0.18s ease-out;
+    }
 
     @keyframes fadeOutCard {
       from { opacity:1; transform:scale(1); }
       to   { opacity:0; transform:scale(0.98); }
     }
-    .fade-out-card {
-      animation:fadeOutCard 0.15s ease-out;
-    }
-    @keyframes fadeInCard {
-      from { opacity:0; transform:translateY(-2px); }
-      to   { opacity:1; transform:translateY(0); }
-    }
-    .fade-in-card {
-      animation:fadeInCard 0.2s ease-out;
-    }
+    .fade-out-card {
+      animation:fadeOutCard 0.15s ease-out;
+    }
+    @keyframes fadeInCard {
+      from { opacity:0; transform:translateY(-2px); }
+      to   { opacity:1; transform:translateY(0); }
+    }
+    .fade-in-card {
+      animation:fadeInCard 0.2s ease-out;
+    }
+
+    @keyframes fadeInAutoRoll {
+      0%   { opacity:0; transform:translateY(10px) scale(0.985); filter:brightness(0.7); }
+      60%  { opacity:0.9; transform:translateY(2px) scale(0.995); filter:brightness(1.15); }
+      100% { opacity:1; transform:translateY(0) scale(1); filter:brightness(1); }
+    }
+
+    @keyframes fadeOutAutoRoll {
+      0%   { opacity:1; transform:scale(1); filter:brightness(1); }
+      40%  { opacity:0.55; transform:scale(0.992); filter:brightness(1.1); }
+      100% { opacity:0; transform:scale(0.98); filter:brightness(0.85); }
+    }
+
+    @keyframes fadeInAutoCard {
+      0%   { opacity:0; transform:translateY(-6px) scale(0.985); filter:brightness(0.75); }
+      70%  { opacity:0.95; transform:translateY(-1px) scale(0.995); filter:brightness(1.15); }
+      100% { opacity:1; transform:translateY(0) scale(1); filter:brightness(1); }
+    }
+
+    @media (min-width: 769px) {
+      body.auto-running .fade-in-fast {
+        animation-name:fadeInAutoRoll;
+        animation-duration:0.6s;
+      }
+      body.auto-running .fade-out-card {
+        animation-name:fadeOutAutoRoll;
+        animation-duration:0.55s;
+      }
+      body.auto-running .fade-in-card {
+        animation-name:fadeInAutoCard;
+        animation-duration:0.6s;
+      }
+    }
 
     .stats-row {
       margin-top:6px;
       font-size:0.9rem;
       display:flex;
       flex-direction:column;
       gap:4px;
     }
 
     #log {
       font-family:ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas,"Liberation Mono","Courier New", monospace;
       font-size:0.75rem;
       max-height:260px;
       overflow-y:auto;
       padding:6px 8px;
       background:#111111;
       border-radius:8px;
       border:1px solid #27272a;
       white-space:pre-wrap;
     }
 
     .tag {
       display:inline-flex;
       align-items:center;
       gap:4px;
